body Geometrie

  proc point(x, y) returns p
  {
      p = new(PointRec)
      p^.x = x;
      p^.y = y;
  }

  proc estDansCercle(p) returns b
  {
     b = (p^.x**2 + p^.y**2 <= 1);
  }

  proc initialisePoint(p)
  {
   # random x [0,1]
   # random y [0,1]
   # Assigner x et y à p
  }

end
